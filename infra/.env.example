# 로그 수집 대상 디렉토리 (각 하위 폴더가 서비스명으로 인식됨)
# 예: /path/to/logs/my-app/app.log → service = "my-app"
LOG_SOURCE_PATH=/path/to/your/logs

# Kafka 외부 접근 시 advertise할 호스트 (Lighthouse EC2 public IP)
# Picook EC2의 Vector가 이 주소로 Kafka에 접근
KAFKA_EXTERNAL_HOST=localhost
