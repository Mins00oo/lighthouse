<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.app.lighthouse.infra.oracle.ApplicationMapper">

    <resultMap id="applicationResultMap" type="com.app.lighthouse.infra.oracle.ApplicationRecord">
        <constructor>
            <idArg column="app_id"       javaType="java.lang.Long"             name="appId"/>
            <arg   column="service_name" javaType="java.lang.String"           name="serviceName"/>
            <arg   column="display_name" javaType="java.lang.String"           name="displayName"/>
            <arg   column="description"  javaType="java.lang.String"           name="description"/>
            <arg   column="status"       javaType="java.lang.String"           name="status"/>
            <arg   column="created_at"   javaType="java.time.LocalDateTime"    name="createdAt"/>
            <arg   column="updated_at"   javaType="java.time.LocalDateTime"    name="updatedAt"/>
        </constructor>
    </resultMap>

    <select id="findAll" resultMap="applicationResultMap">
        SELECT app_id, service_name, display_name, description, status, created_at, updated_at
        FROM lh_application
        <where>
            <if test="status != null and status != ''">
                status = #{status}
            </if>
        </where>
        ORDER BY created_at DESC
    </select>

    <select id="findById" resultMap="applicationResultMap">
        SELECT app_id, service_name, display_name, description, status, created_at, updated_at
        FROM lh_application
        WHERE app_id = #{appId}
    </select>

    <select id="findByServiceName" resultMap="applicationResultMap">
        SELECT app_id, service_name, display_name, description, status, created_at, updated_at
        FROM lh_application
        WHERE service_name = #{serviceName}
    </select>

    <insert id="insert">
        INSERT INTO lh_application (service_name, display_name, description)
        VALUES (#{serviceName}, #{displayName}, #{description, jdbcType=VARCHAR})
    </insert>

    <update id="update">
        UPDATE lh_application
        SET display_name = #{displayName},
            description  = #{description, jdbcType=VARCHAR},
            status       = #{status},
            updated_at   = SYSTIMESTAMP
        WHERE app_id = #{appId}
    </update>

    <delete id="delete">
        DELETE FROM lh_application WHERE app_id = #{appId}
    </delete>

</mapper>
